# Reference only — not imported, not run

`verification_team_recovered_lines_1-500.py` is the team's deterministic
VerificationAgent (`_score()`, `Facts`, `_repo_matches()`, `_match_result()`,
`SINGLE_SOURCE_CEILING` / `MISSING_REPO_CEILING`). It was reviewed in chat but
never committed to any repo. Lines 1–500 of 571 were recovered; the rest is
missing, including `_describe(cluster)` (called once, to build the agentic
loop's user message) and presumably the CLI.

Kept here so it is backed up. No code imports it. A plain `import` statement
cannot name it (the hyphens in the filename are a syntax error). `importlib`
could still locate it -- a folder without `__init__.py` is a namespace package
-- but loading fails today anyway, because it does not match the current
`schemas.py`: it expects `ReasoningStep`, `Evidence.kind/.verified` (with `tier="tool"`), and
`VerifiedTrend.verified_source_count/.repo_exists/.claim_verified/.reasoning/.mode`.

Evidence it is the code `test_chain.py` sections 1–2 were written for: run
against this file in memory (with a stand-in `ReasoningStep`), the two
sections that SKIP on the committed agent pass 13/13.

Restoring it — combined with PR #1's gates — is pending, and must happen
before the next capture run. Do not edit this file; restore from it.

## Complete file (added later)

`verification_team_1-500_plus_reconstructed_tail.py` = the recovered lines
1–500, byte-identical (verified with cmp after normalising line endings; zero
lines changed or removed), plus a RECONSTRUCTED tail: `_describe()` and
`main()`, rebuilt 2026-09-21 from how the rest of the file uses them. The tail
is a reconstruction, not the lost original (596 lines vs the original 571).
This is the source for restoring the deterministic verifier into
`02_src/agents/verification.py`.
